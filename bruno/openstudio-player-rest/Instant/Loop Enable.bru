meta {
  name: Loop Enable
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/instant/{{instantId}}/loop
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "enabled": true
  }
}

tests {
  test("enables instant loop", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
