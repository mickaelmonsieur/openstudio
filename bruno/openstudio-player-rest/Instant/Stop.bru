meta {
  name: Stop
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/instant/{{instantId}}/stop
  body: none
  auth: none
}

tests {
  test("stops instant slot", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
