meta {
  name: Play
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/instant/{{instantId}}/play
  body: none
  auth: none
}

tests {
  test("plays instant slot", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
