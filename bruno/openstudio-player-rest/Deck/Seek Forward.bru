meta {
  name: Seek Forward
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/deck/seek
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "offset_ms": {{seekForwardMs}}
  }
}

tests {
  test("seeks deck forward", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
