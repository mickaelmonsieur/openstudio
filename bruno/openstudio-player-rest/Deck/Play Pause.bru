meta {
  name: Play Pause
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/deck/play-pause
  body: none
  auth: none
}

tests {
  test("toggles deck play/pause", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
