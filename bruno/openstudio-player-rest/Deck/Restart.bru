meta {
  name: Restart
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/deck/restart
  body: none
  auth: none
}

tests {
  test("restarts deck", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
