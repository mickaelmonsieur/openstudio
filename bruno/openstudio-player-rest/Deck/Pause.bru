meta {
  name: Pause
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/deck/pause
  body: none
  auth: none
}

tests {
  test("pauses deck", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
