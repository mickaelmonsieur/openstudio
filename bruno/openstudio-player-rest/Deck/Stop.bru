meta {
  name: Stop
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/deck/stop
  body: none
  auth: none
}

tests {
  test("stops deck", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
