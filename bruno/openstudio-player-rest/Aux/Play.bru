meta {
  name: Play
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/aux/{{auxId}}/play
  body: none
  auth: none
}

tests {
  test("plays aux player", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
