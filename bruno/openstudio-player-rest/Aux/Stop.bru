meta {
  name: Stop
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/aux/{{auxId}}/stop
  body: none
  auth: none
}

tests {
  test("stops aux player", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
