meta {
  name: Loop Enable
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/aux/{{auxId}}/loop
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "enabled": true
  }
}

tests {
  test("enables aux loop", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
