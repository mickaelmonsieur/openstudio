meta {
  name: Toggle Queue Preview
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/deck/queue/{{queueId}}/preview/toggle
  body: none
  auth: none
}

tests {
  test("toggles queue preview", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
