meta {
  name: Stop Preview
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/deck/preview/stop
  body: none
  auth: none
}

tests {
  test("stops preview", function() {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
